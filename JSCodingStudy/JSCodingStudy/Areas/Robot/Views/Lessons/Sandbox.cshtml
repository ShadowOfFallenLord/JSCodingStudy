
@{
    ViewBag.Title = "Sandbox";
    ViewBag.IsLesson = false;
    ViewBag.LessonId = -1;
}

@section task {
    Песочница.
}

@section help {
    var help_node = help.ParseHelp(help.GetHelp(true, true, true));
}

@section field_init {
    var RobotAPI = RAPI.CreateFullRobotAPI(10, 10, true, 5, 5, html_elements.LogDisplay, delay);
}

@section user_code {
    @{
        string code = @"while(Robot.Check(Directions.Up, Checks.Void))
{
Robot.Move(Directions.Up);
Robot.Draw();
}
Robot.Move(Directions.Right);
for(var i = 0; i < 4; i++)
{
Robot.Move(Directions.Up);
Robot.Draw();
}";
    }
    html_elements.TextInput.val(`@Html.Raw(code)`);
}
